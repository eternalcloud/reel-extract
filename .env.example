# Required once the app is deployed.
APP_ORIGIN=https://your-app.example
WORK_SESSION_SIGNING_KEY=replace-with-at-least-32-random-characters

# Phase 0 currently uses development-only process memory.
# Production deliberately fails closed until the durable Supabase adapter lands.
